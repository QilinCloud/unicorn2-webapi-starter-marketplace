# Tools Components

- `sign_request.php`: creates and optionally sends signed ApiWeb test requests.
- `run_conformance.php`: executes the happy-path and negative ApiWeb contract tests.
- `mock_marketplace.php`: local mock marketplace for adapter development in real mode.
