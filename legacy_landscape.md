# Starter Legacy Landscape

The historical ApiWeb sample mixed protocol, domain objects and example marketplace behavior.

This starter separates the stable ApiWeb protocol from the replaceable marketplace adapter.

