# Starter Domain Notes

Unicorn owns the ApiWeb contract. The third-party connector owns marketplace communication.

The connector must return clear ApiWeb errors for marketplace failures and continue with later objects when one object fails.

